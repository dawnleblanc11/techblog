<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header class= "hero">
      <h1 class="app-title"></i>Tech Blog</h1>
      <div>
          <nav class= "nav">
            <a class="link card" href="/">home</a>
            <a class="link card" href="/dashboard">dashboard</a>
            {{#if loggedIn}}
            <a class="link card" href="/" id="logout">logout</a>
            {{else}}
            <a class="link card" href="/login">login to add posts or comment</a>
                {{/if}}
            </nav>
        </div>
    </header>
    <main>
      {{{ body }}}
    </main>
  </div>
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>
</html>